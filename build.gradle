// Top-level build file
plugins { id 'com.github.ben-manes.versions' version '0.46.0' apply false }
buildscript { repositories { google(); mavenCentral() } }
allprojects { repositories { google(); mavenCentral() } }
